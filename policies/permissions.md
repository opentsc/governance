# 权限政策

- Organization Owner 保持极少；当前由创始人 `cat9999aaa` 持有。
- 组织默认仓库权限为 `none`，通过 Team 或单仓库角色授予权限。
- 普通成员默认不能创建组织仓库。
- Repository Admin 是动态任务权限，目标结束后回收。
- Agent 默认只读；任何写权限都必须写明仓库、动作、期限和撤销方法。
- Secrets、成员管理、仓库删除、账单和安全降级不得由 Agent 单独执行。
- 每季度以及成员/项目状态变化时进行权限复核。
